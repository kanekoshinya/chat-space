.side-bar
 .side-bar__user-name
  .top-items
   .top-items__name
    = current_user.name
   .top-items__icons
    = link_to new_group_path, class: "top-items__icons--edit  " do
     = icon('fas', 'edit')
    = link_to edit_user_path(current_user), class: "top-items__icons--cog  " do
     = icon('fas', 'cog')
 .side-bar__group-list
  - current_user.groups.each do |group|
   .group-items
    = link_to '#' do
     .group-items__name
      = group.name
     .group-items__message
      まだメッセージがありません